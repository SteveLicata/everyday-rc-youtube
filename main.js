$(document).ready(function(){

  // materialize collapsable nav
  $(".button-collapse").sideNav();

  // materialize slider
  $('.slider').slider({
    full_width: true,
    interval: 3000
  });

});
